package atc.atcsim;

/**
 * The AtcLogger class automatically logs all the actions made by the user (Atc Controller) as strings with timestamps
 */
public class AtcLogger {
    // TODO: add lines for all methods so they automatically log all changes to the log data structure or class,
    //  whatever is used, I think this class contain logging methods that can be extended from in other classes to
    //  easily log to some data structure
}
